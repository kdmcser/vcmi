#ifndef MASK_H
#define MASK_H
#include <cstddef>

// Auto-generated mask array from MOD_PASSWORD_MASK
constexpr unsigned char mask[] = { @MASK_BYTES_STR@ };
constexpr size_t mask_length = sizeof(mask)/sizeof(mask[0]);

#endif // MASK_H
